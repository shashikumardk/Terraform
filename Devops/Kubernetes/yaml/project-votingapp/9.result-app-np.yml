apiVersion: v1
kind: Service
metadata:
  name: myservice-nodeport
  labels:
    type: nodeport
spec:
  type: NodePort
  selector:
    type: prod
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000